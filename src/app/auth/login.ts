// É uma model que representa os dados de login do usuário e senha. É somente o o nome do usuário e a senha que estão sendo mandados para o Backend;
export class Login {
  username!: string;
  password!: string;
}
